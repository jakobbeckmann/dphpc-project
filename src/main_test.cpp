//
// Created by mathee on 19.10.2017.
//

#include <iostream>
#include "FileWriter.h"

/*
int main_test(int argc, char const *argv[]) {

    FileWriter fileWriter;
    std::string fileName = "testfile";
    fileWriter.setBaseName(fileName);
    fileWriter.updateFileName();

    for (int i=0; i < 10; i++) {
        Point point = Point(i, 2*i);
        fileWriter.appendPointToFile(point, 1);
        std::cout << "Appended..." << std::endl;
    }

    return 0;
}
 */